fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
# IAP JWT verification (iap.py) — only used at runtime when IAP is enabled.
# `requests` is google-auth's transport for id_token.verify_token (an optional
# google-auth extra, not pulled in automatically) — without it verification
# raises and the agent routes fail closed with a 401.
google-auth>=2.28
requests>=2.31
# Admin API (app/admin.py): ACL docs in Firestore. httpx (above) covers the
# Agent Registry REST listing.
google-cloud-firestore>=2.16
# Named front-ai-logs log with trace attribute (tracing.setup_logging).
google-cloud-logging>=3.10
